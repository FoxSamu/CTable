plugins {
    id 'java'
}

group 'net.rgsw'
version '1.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

build {

}

jar {
    archiveName = rootProject.name + "-v" + rootProject.version + ".jar"
    destinationDir = new File( "build/libs" )
}
